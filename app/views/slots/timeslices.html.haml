%div
    %h4 timeslots
    -  x = ['09:00','09:30','10.00','10.30','11.00','11.30',
            '12.00','12.30','13.00','13.30','14.00',
            '14.30','15.00','15.30','16.00','16.30',
            // my existing slots shuld be a variable
            // use a map function to pull out their times and compare them. 
            // does this exist here? if so, do something else. 
        '17.00','17.30',]
    - (0..17).each do |i|
        %input#i{name: "timeslice",type: "checkbox"}/
        %label{for: i} #{x[i]} &nbsp&nbsp
