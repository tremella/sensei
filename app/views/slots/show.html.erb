<h1>Slot</h1>
<p>
    id: <%= @slot.id %><br> 
    coach: <%= @slot.coach_id %><br>
    starts: <%= @slot.start.strftime("%H:%M%P %a %b %d") %><br> 
    ends: <%= @slot.end.strftime("%H:%M%P %a %b %d") %>
    <% if !(current_user.is_coach) %>
        <button> book </button>
    <% end %>
</p>