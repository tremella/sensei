console.log('Hello! I am in the timeslice code!')
console.log( 'NOW, parse them to see if any are sat sun, so we can ignore those')
console.log('remember, timeslices only for 9am to 6pm')


document.addEventListener('DOMContentLoaded', function(){
  
    var allDays = document.querySelectorAll("td");
    allDays.forEach(element => element.addEventListener('click', ()=>{
        yyyy = element.innerText.slice(0,4)
        mm = element.innerText.slice(5,7)
        dd = element.innerText.slice(8,10)

        const day = new Date(yyyy, mm, dd);
        isWeekend = (day.getDay() == 1 || day.getDay() == 2)

        if (isWeekend == false) {
            console.log(element.innerText)
        }
    }))








});